import main
import config
import utils

def test_main():
    test = main
    assert main == test

def test_config():
    test = config
    assert config == test

def test_utils():
    test = utils
    assert utils == test
